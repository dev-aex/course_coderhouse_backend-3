// Users

export const getAllUsersErrorInfo = () => {
  return `Error getting all the users from the database`;
};

export const generateUsersMockedDataErrorInfo = () => {
  return `Error generating users`;
};

export const getOneUserErrorInfo = () => {
  return `Error getting one user`;
};

// Pets

export const generatePetsMockedDataErrorInfo = () => {
  return `Error generating pets`;
};

export const getOnePetErrorInfo = () => {
  return `Error getting one pet`;
};

export const deleteOnePetErrorInfo = () => {
  return `Error deleting one pet`;
};

export const updateOnePetErrorInfo = () => {
  return `Error updating one pet`;
};

export const getAllPetsErrorInfo = () => {
  return `Error getting all pets`;
};

export const PetExistErrorInfo = () => {
  return 'Pet is already adopted';
};

// Adoptions

export const getAllAdoptionsErrorInfo = () => {
  return `Error getting all adoptions`;
};

export const getOneAdoptionErrorInfo = () => {
  return `Error getting one adoption`;
};
